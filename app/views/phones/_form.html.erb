<%= form_with(model: phone, html: { class: 'form-style-6' }) do |form| %>
  <% if phone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
        <% phone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label t :'activerecord.models.phone.one', style: "display: block" %>
    <%= form.text_field :phone %>
  </div>

  <div>
    <%= form.label t :'activerecord.models.contact.one', style: "display: block" %>
    <%#= form.text_field :contact_id %>
    <%= collection_select(:phone, :contact_id, @contact_options, :id, :name) %>

  </div>

  <div class="dflex">
    <%= form.submit t :'phones.new', class: "btn btn-default" %>
    <%= link_to (t :'go_back'), phones_path, class: "btn btn-default"%>

  </div>
<% end %>
